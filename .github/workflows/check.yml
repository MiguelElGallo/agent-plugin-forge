name: Check

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  quality:
    name: quality
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"
      - uses: astral-sh/setup-uv@11f9893b081a58869d3b5fccaea48c9e9e46f990 # v8.3.2
        with:
          enable-cache: true
      - run: uv sync --locked
      - run: uv run ruff check .
      - run: uv run ruff format --check .
      - run: uv run ty check
      - name: Validate skill branch name
        if: github.event_name == 'pull_request'
        env:
          PR_BRANCH: ${{ github.head_ref }}
          PR_BASE: ${{ github.base_ref }}
        run: uv run forge pr-scope --branch "$PR_BRANCH" --base "$PR_BASE"
      - run: uv run forge check
      - run: uv run zensical build --clean --strict

  tests:
    name: tests (${{ matrix.os }}${{ matrix.suffix }})
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python: ["3.12"]
        include:
          - os: ubuntu-latest
            python: "3.11"
            suffix: ", Python 3.11"
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: ${{ matrix.python }}
      - uses: astral-sh/setup-uv@11f9893b081a58869d3b5fccaea48c9e9e46f990 # v8.3.2
        with:
          enable-cache: true
      - run: uv sync --locked
      - run: uv run pytest
      - name: Build and smoke-test the installed import and update workflow
        if: matrix.python == '3.12'
        env:
          FORGE_PR_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: uv run python scripts/smoke_wheel.py --output-dir "${{ runner.temp }}/forge-release"
      - name: Retain the tested distributions and hashes
        if: matrix.python == '3.12'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: distributions-${{ matrix.os }}-python-${{ matrix.python }}-${{ github.sha }}
          path: ${{ runner.temp }}/forge-release/
          if-no-files-found: error
          retention-days: 14
          compression-level: 0
